name: 🛡️ Cyber News Auto-Publisher

on:
  schedule:
    - cron: '0 */6 * * *'  # 每 6 小時執行一次 (UTC 時間)
  workflow_dispatch:      # 允許手動按按鈕觸發

permissions:
  contents: write         # 給予機器人寫入權限 (重要！)

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run Cyber News Agent
        env:
          # 🔥 修正點：這裡必須明確把 Secret 傳遞給環境變數
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          AI_PROMPT_TEMPLATE: ${{ secrets.AI_PROMPT_TEMPLATE }} # 新增這一行！
        run: |
          # 檢查環境變數是否正確讀取 (為了安全只印出前幾個字或長度)
          python -c "import os; print(f'🔍 AI_PROMPT_TEMPLATE Status: {len(os.getenv(\"AI_PROMPT_TEMPLATE\", \"\")) > 0}')"
          
          # 執行主程式
          python main.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "CyberNewsBot"
          git config --global user.email "bot@github.com"
          
          # 1. 嘗試加入新文章
          git add docs/_posts/*.md 2>/dev/null || true
          git add data/processed_urls.txt 2>/dev/null || true
          
          # 2. 檢查 Git 狀態
          if git diff --staged --quiet; then
            echo "🚫 沒有新文章生成，跳過 Commit 步驟。" 
            exit 0
          else
            echo "✅ 發現新文章，正在推送到 GitHub..."
            git commit -m "📝 AI 自動發佈資安報告 $(date +'%Y-%m-%d %H:%M')"
            git push
          fi