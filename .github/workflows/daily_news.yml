name: 🛡️ Cyber News Auto-Publisher

on:
  schedule:
    - cron: '0 */6 * * *'  # 每 6 小時執行一次 (UTC 時間)
  workflow_dispatch:      # 允許手動按按鈕觸發

permissions:
  contents: write         # 給予機器人寫入權限 (重要！)

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run Cyber News Agent
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: |
          python main.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "CyberNewsBot"
          git config --global user.email "bot@github.com"
          
          # 1. 嘗試加入新文章 (忽略錯誤訊息)
          # 使用 2>/dev/null 隱藏 "沒有檔案" 的報錯
          git add docs/_posts/*.md 2>/dev/null || true
          git add data/processed_urls.txt 2>/dev/null || true
          
          # 2. 檢查 Git 狀態
          if git diff --staged --quiet; then
            echo "🚫 沒有新文章生成，跳過 Commit 步驟。" 
            exit 0
          else
            echo "✅ 發現新文章，正在推送到 GitHub..."
            git commit -m "📝 AI 自動發佈資安報告 $(date +'%Y-%m-%d %H:%M')"
            git push
          fi